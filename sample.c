#include <stdio.h>
#include "utils.c"

int main()
{
    printf("kaise hain  ICS 23/IITJ ?");
    int a = 5;
    int b = 3;
    int c = sum(a,b);
    printf("sum of %d and %d is %d",a,b,c);
    return 0;
}
